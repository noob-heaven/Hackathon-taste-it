import type { Login } from "../@types/login";

const getUser = async () => {
  const response = await fetch('', {
    head: ,
    body: ,
  });

  if (!response.ok) {

  }




  return

}



export default getUser;